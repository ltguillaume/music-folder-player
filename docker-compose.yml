services:
  mfp:
    environment:
      - ADDRESS=
      - PORT=
    volumes:
      - source: /YOUR/MUSIC/FOLDER
        target: /app/srv/library
        type: bind
        read_only: true
      - source: mfp
        target: /app/cfg
        type: volume
    build:
      context: https://github.com/ltGuillaume/MusicFolderPlayer.git#docker
    container_name: mfp
    image: mfp
    network_mode: bridge
    restart: unless-stopped
volumes:
  mfp: